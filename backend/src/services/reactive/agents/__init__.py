"""Concrete agents that run inside the Self-Heal Envelope.

Each module here implements the ``Agent`` protocol (see ``reactive/agent.py``) and
is registered with the ``AgentRegistry``. Adding a capability = add one module
here + one registry line, no scheduler surgery.
"""
