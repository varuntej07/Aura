<?xml version="1.0" encoding="utf-8"?>
<!-- The widget's cream pill surface: rounded rectangle with a soft warm border,
     matching the app's FauxGlassCard tiles. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/aura_widget_surface" />
    <corners android:radius="24dp" />
    <stroke android:width="1dp" android:color="@color/aura_widget_border" />
</shape>
